import { css } from "lit";

const styles = css`
  :host {
    display: flex;
    flex-direction: column;
    margin: 24px;
  }
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-family: "Archivo", sans-serif;
  }
`;
export default styles;
